#Ignore Object and Archive files that may be the product of building the code.
*.[oa]

#Ignore all files whose names end with a tilde (~)
# These are files used by many text editors such as Emacs to mark temporary files.
*~

#Ignore file(s).
.gitignore

# node_modules/ folder need not be pushed to Git server.
# It will be big and unnecessary to push it.
# All the package details are there in package.json file.
# We only need that file to be pushed to Git server to later build and run the application.
node_modules/

# .cache/ is added to ignore temporary cache files generated by bundlers.
# If, for your bundler like Parcel or Webpack, the cache name is different.
# So rename this folder name in .gitignore.
.cache/

# dist/ is the common name to store the output of a build process.
# We do not have to push the output of a build to Git.
# If we need to deploy the app, we take a fresh build at that time from source files.
dist/

# .env contains secure environment variables and values.
# We do not want it to be shared with others.
.env


.DS_Store

# coverage/ folder is generated as part of code coverage testing.
# That need not be there in the Git.
coverage/

# .vscode/ folder is created by Visual Studio Code IDE.
# It stores project specific settings.
# We do not want this folder to be pushed to Git server.
.vscode/

# dependencies
/.pnp
.pnp.js

# production
/build

package-lock.js

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.hintrc

package-lock.json
package.json

# In General I want to ignore Public folder.
public/